apiVersion: v1
kind: Service
metadata:
  name: {{ include "rickmorty-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "rickmorty-api.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
